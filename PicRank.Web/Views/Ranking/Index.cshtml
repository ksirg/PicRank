@model IList<PicRank.Web.Models.PictureView>
@{
    ViewBag.Title = "Index";
}

<div class="msgBox">
@ViewBag.SimpleRankingMsg
</div>

<p>
    
    @using (Html.BeginForm("Create", "Ranking", FormMethod.Post))
    {
        <input type="submit" value="Przelicz ranking" />
    }
</p>

<h3>
Wybierz zdjęcie dla którego ma być wyświetlony ranking</h3>

@*

@foreach (var item in Model)
{
    
    <div class="picBox">
        <a href="@Url.Action("Ranking", new { id = item.Id })" title="">
            <img style="margin:2px auto" src="@Request.Url.GetLeftPart(UriPartial.Authority)/@item.FullPath" alt="picture @item.Id" width="180px" />
        </a>

    </div>
    
}
*@


@{
    for (int i = 0; i < Model.Count; i++)
    {
        var item = Model[i];

        if (i % 4 == 0)
        { 
           @: <div class="picRow">
        }

        <div class="picBox" >
         <a href="@Url.Action("Ranking", new { id = item.Id })" title="">
            <img style="margin:2px auto" src="@Request.Url.GetLeftPart(UriPartial.Authority)/@item.FullPath" alt="picture @item.Id" width="180px" />
        </a>
                
                <div class="picSummary">
                
                </div>
        </div>
        
       if( ((i+1)%4 ==0) || i==Model.Count-1 ){
        @: </div>
        }

    }
}
<div style="clear: both">
</div>